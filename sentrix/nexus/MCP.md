# MCP Servers — nexus

None configured.

nexus's role is engineering flow management — managing the Kanban board, writing acceptance criteria, enforcing WIP limits, and coordinating developers. All of these functions are handled by paperclip (scheduling, inter-agent comms, Kanban, filesystem). GitHub/PR access is covered by the `gh` CLI which is available directly in the shell environment.
